linters:
  enable-all: true
  disable:
    - gochecknoglobals
    - gomnd
    - nlreturn
    - wsl
    # TODO: maybe re-enable these
    - exhaustruct
    - godox
    - varnamelen
    # TODO: fix error handling
    - goerr113
    - wrapcheck
    - errorlint
    - errname

    # Deprecated linters
    - deadcode
    - exhaustivestruct
    - golint
    - ifshort
    - interfacer
    - maligned
    - nosnakecase
    - scopelint
    - structcheck
    - varcheck

linters-settings:
  govet:
    check-shadowing: true
  gci:
    local-prefixes: github.com/greenboxal/aip/tools/ircproxy/irc
